import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import * as faker from "faker";
import Card from ".";

<Meta
	title="Components/Layout/Card"
	component={Card}
	argTypes={{
		title: {
			description: "The title header for the card.",
			type: {
				required: true,
			},
			table: {
				category: "Main",
			},
		},
		children: {
			description: "Content to appear when accordion is open.",
			type: {
				required: true,
				summary: "renderable",
			},
			table: {
				category: "Main",
			},
		},
		subtitle: {
			description: "The subtitle header for the card.",
			type: {
				required: true,
			},
			table: {
				category: "Secondary",
			},
		},
	}}
	args={{
		title: "Test card",
		subtitle: "with a simple subtitle",
		children: faker.lorem.paragraph(3),
	}}
/>

export const Template = (args) => <Card {...args} />;

# Card
---

<Canvas>
	<Story name="Card">
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable of={Card} />